{
  "permissions": {
    "allow": [
      "PowerShell(Get-ChildItem .\\\\tmp-init -Force | Where-Object { $_.Name -ne \"node_modules\" -and $_.Name -ne \".git\" } | ForEach-Object { Move-Item $_.FullName . -Force }; Write-Host \"Done moving files\")",
      "PowerShell(npm install 2>&1)",
      "mcp__Claude_Preview__preview_start",
      "Bash(npx tsc *)",
      "Bash(mkdir -p passreg-src)",
      "Bash(unzip -o PassReg.zip -d passreg-src)",
      "Bash(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3001/)",
      "Bash(npm run *)"
    ]
  }
}
